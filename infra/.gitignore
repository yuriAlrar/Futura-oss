# Terraform files
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform/
# .terraform.lock.hcl は意図的にコミットする（provider バージョンを環境間で固定するため）
backend.hcl
tfplan
crash.log
crash.*.log

# Lambda deployment artifacts
*.zip
placeholder.zip

# OS files
.DS_Store
Thumbs.db